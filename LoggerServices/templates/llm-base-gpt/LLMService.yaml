apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{.Values.LLMBaseGPTService.DNS}}
  labels:
    app: {{.Values.LLMBaseGPTService.DNS}}
spec:
  replicas: {{.Values.Gateway.DeploymentReplicas}}
  selector:
    matchLabels:
      app: {{.Values.LLMBaseGPTService.DNS}}
  template:
    metadata:
      labels:
        app: {{.Values.LLMBaseGPTService.DNS}}
    spec:
      containers:
        - name: gateway
          image:  {{.Values.LLMBaseGPTService.Image}}
          imagePullPolicy: {{.Values.Policy.ImagePullPolicy}}
          ports:
            - containerPort: {{.Values.LLMBaseGPTService.RestPort}}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
      restartPolicy: {{.Values.Policy.RestartPolicy}}
      securityContext:
        runAsNonRoot: false


