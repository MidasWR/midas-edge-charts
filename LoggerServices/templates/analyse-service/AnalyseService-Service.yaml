apiVersion: v1
kind: Service
metadata:
  name: {{.Values.AnalyseService.DNS}}
spec:
  selector:
    app: {{.Values.AnalyseService.DNS}}
  ports:
    - protocol: TCP
      port: {{.Values.AnalyseService.RestPort}}
      targetPort: {{.Values.All.targetPort}}
  type: LoadBalancer
  